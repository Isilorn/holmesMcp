# Session J8-4bis — Plan (à exécuter)

**Date prévue** : 2026-05-06 (ou suivant)
**Branche** : `develop`
**Prérequis** : snapshot Proxmox fait avant J8-4 — valide pour cette session aussi

---

## Contexte : bug CLOSE-WAIT FastMCP (découvert J8-4)

Toute connexion HTTP interrompue de façon abrupte (timeout OS / SIGKILL client) laisse un
socket en état `CLOSE-WAIT` côté serveur uvicorn. Ces sockets accumulent des données en
Recv-Q (≈ 470 bytes), ce qui signale epoll en boucle → asyncio spin loop → daemon à 76%
CPU, ne répond plus.

**Cause racine** : `StreamableHTTPServerTransport` (FastMCP / uvicorn) ne gère pas le
passage `ESTABLISHED → CLOSE-WAIT` pour les connexions SSE. Bug upstream non corrigé.

**Mitigation choisie** : watchdog asyncio interne au daemon, Niveau 2 (validé PO).

### Pourquoi nos anciens smoke tests déclenchaient le bug

Deux problèmes distincts :

### Problème 1 — `timeout` OS = SIGKILL déguisé

```bash
timeout 20 python3 script.py   # OS envoie SIGKILL après 20s
```

SIGKILL ne laisse aucune chance au code Python de se nettoyer — les `async with` ne
s'exécutent pas, les sockets restent ouverts côté serveur → CLOSE-WAIT.

### Problème 2 — lecture dans le mauvais canal

Le protocole MCP Streamable HTTP est **bi-canal** :

- Le client envoie ses requêtes via **POST /mcp**
- Le serveur envoie ses réponses via un **GET SSE** (canal séparé)

Nos scripts `httpx`/`curl` attendaient la réponse dans le body du POST — il était vide.
La réponse `initialize` partait vers un GET SSE qu'on n'avait jamais ouvert. D'où les
0 bytes reçus et les timeouts.

### Un smoke test HTTP propre, c'est quoi

- Utiliser le client officiel `mcp` (library) qui gère les deux canaux automatiquement
- Remplacer `timeout` OS par `asyncio.wait_for()` — la CancelledError remonte dans les
  context managers `async with` qui ferment les connexions avec FIN/ACK propres

```python
async def smoke():
    async with streamablehttp_client(...) as (r, w, _):
        async with ClientSession(r, w) as session:
            await asyncio.wait_for(session.initialize(), timeout=10.0)
            result = await asyncio.wait_for(session.list_tools(), timeout=10.0)
            # Sortie des `async with` → connexion fermée proprement (FIN/ACK)
```

---

## Objectif J8-4bis

1. **Garde-fou asyncio Niveau 2** dans `holmesMcpd.py` — tâche de fond qui scanne
   `/proc/self/net/tcp` toutes les 15 s, nettoie les sockets CLOSE-WAIT via
   `loop.remove_reader(fd)` + `loop.remove_writer(fd)` + `os.close(fd)`, et **loggue
   chaque intervention**.

2. **Réécriture des smoke tests HTTP** — client officiel `mcp` + `asyncio.wait_for()`
   (pas `timeout` OS, pas `build_mcp()`).

3. **Smoke test reproduisant le CLOSE-WAIT** — valide que le garde-fou détecte et nettoie.

4. **Dérouler tous les smoke tests** sur la box, vérifier CPU daemon avant/après.

---

## 1. Implémentation garde-fou (`holmesMcpd.py`)

### Fichier cible

`resources/holmesMcpd/holmesMcpd.py`

> Vérifier les imports existants avant d'appliquer — le fichier importe déjà `os` et
> `Path`. Ajouter uniquement `import asyncio` s'il manque.

### Fonctions à ajouter (avant `main()`)

```python
async def _watchdog_close_wait(port: int) -> None:
    """Nettoie les sockets CLOSE-WAIT laissés par des clients MCP déconnectés abruptement.

    Bug FastMCP : StreamableHTTPServerTransport ne ferme pas les sockets quand le client
    envoie un FIN mid-SSE-stream. Les sockets restent en CLOSE-WAIT avec données en Recv-Q,
    epoll les signale en boucle → spin CPU. Ce watchdog les nettoie toutes les 15 s.
    """
    while True:
        await asyncio.sleep(15)
        inodes = _find_close_wait_inodes(port)
        if not inodes:
            continue
        log.warning('close_wait_cleanup', count=len(inodes), port=port)
        _close_sockets_by_inode(inodes)


def _find_close_wait_inodes(port: int) -> set[int]:
    CLOSE_WAIT = 8
    inodes: set[int] = set()
    try:
        with open('/proc/self/net/tcp') as f:
            for line in f.readlines()[1:]:
                parts = line.split()
                if len(parts) < 10:
                    continue
                local_port = int(parts[1].split(':')[1], 16)
                state = int(parts[3], 16)
                inode = int(parts[9])
                if local_port == port and state == CLOSE_WAIT:
                    inodes.add(inode)
    except OSError:
        pass
    return inodes


def _close_sockets_by_inode(inodes: set[int]) -> None:
    loop = asyncio.get_running_loop()
    closed = 0
    for entry in Path('/proc/self/fd').iterdir():
        try:
            target = os.readlink(entry)
            if 'socket:[' not in target:
                continue
            inode = int(target[8:-1])
            if inode not in inodes:
                continue
            fd = int(entry.name)
            try:
                loop.remove_reader(fd)
            except Exception:
                pass
            try:
                loop.remove_writer(fd)
            except Exception:
                pass
            os.close(fd)
            closed += 1
        except OSError:
            pass
    if closed:
        log.info('close_wait_sockets_closed', closed=closed)
```

### Modification de `main()`

Juste après `log.info('daemon_listening', ...)` et **avant** `uvicorn.run(...)` :

```python
        asyncio.get_event_loop().create_task(_watchdog_close_wait(ARGS.port))
```

---

## 2. Smoke test propre (HTTP)

Script à créer dans `tests/smoke/` (ou `/tmp/` pour la box) :

```python
# smoke_mcp_clean.py
"""Smoke test MCP propre — client officiel + asyncio.wait_for (pas de timeout OS)."""
import asyncio
import sys

from mcp.client.session import ClientSession
from mcp.client.streamable_http import streamablehttp_client


async def smoke(url: str, token: str) -> None:
    headers = {'Authorization': f'Bearer {token}'}
    async with streamablehttp_client(url, headers=headers) as (r, w, _):
        async with ClientSession(r, w) as session:
            await asyncio.wait_for(session.initialize(), timeout=10.0)
            result = await asyncio.wait_for(session.list_tools(), timeout=10.0)
            tools = sorted(t.name for t in result.tools)
            print(f'{len(tools)} tools: {tools}')
            assert len(tools) == 27, f'Attendu 27 tools, obtenu {len(tools)}'
    # Ici les deux `async with` sont sortis → FIN/ACK envoyés → pas de CLOSE-WAIT
    print('OK — connexion fermée proprement')


if __name__ == '__main__':
    url = sys.argv[1]   # ex: http://127.0.0.1:8765/mcp
    token = sys.argv[2] # token Holmes MCP (--apikey dans cmdline daemon)
    asyncio.run(smoke(url, token))
```

Usage sur la box :

```bash
sudo -u www-data python3 /tmp/smoke_mcp_clean.py http://127.0.0.1:8765/mcp <TOKEN>
```

---

## 3. Smoke test reproduisant le CLOSE-WAIT (pour valider le garde-fou)

Ce script se connecte et abandonne **sans** fermer proprement — reproduit exactement
ce que faisaient nos anciens scripts avec `timeout` OS :

```python
# smoke_close_wait_trigger.py
"""Reproduit le bug CLOSE-WAIT pour valider le watchdog.
Envoie un POST /mcp incomplet puis ferme le socket avec RST (SO_LINGER=0).
"""
import socket
import sys
import time

HOST = '127.0.0.1'
PORT = int(sys.argv[1]) if len(sys.argv) > 1 else 8765
TOKEN = sys.argv[2] if len(sys.argv) > 2 else ''

s = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
s.connect((HOST, PORT))

# POST avec Content-Length annoncé mais body tronqué — uvicorn commence à lire
request = (
    f'POST /mcp HTTP/1.1\r\n'
    f'Host: {HOST}:{PORT}\r\n'
    f'Authorization: Bearer {TOKEN}\r\n'
    f'Content-Type: application/json\r\n'
    f'Content-Length: 200\r\n'
    f'\r\n'
    f'{{"jsonrpc":"2.0","id":1,"method":"initialize","params":{{'
    # body tronqué intentionnellement
)
s.sendall(request.encode())
time.sleep(0.5)

# RST = fermeture abrupte sans FIN → CLOSE-WAIT côté serveur garanti
s.setsockopt(socket.SOL_SOCKET, socket.SO_LINGER, b'\x01\x00\x00\x00\x00\x00\x00\x00')
s.close()

print(f'Socket fermé avec RST sur {HOST}:{PORT}')
print('Attendre max 30s, puis vérifier :')
print('  1. CPU daemon < 20%')
print('  2. Log daemon contient close_wait_cleanup')
```

Usage sur la box :
```bash
python3 /tmp/smoke_close_wait_trigger.py 8765 <TOKEN>
# Attendre 15-30 s
ps aux | grep holmesMcpd | grep -v grep          # CPU < 20% ?
sudo tail -50 /var/log/jeedom/holmesMcp.log | grep close_wait
```

---

## 4. Procédure complète sur la box

```bash
# 0. Sudo temporaire (depuis bluemoon)
dev/add-sudo-temp.sh

# 1. Ruff + tests unitaires (local, avant rsync)
ruff check resources/holmesMcpd/holmesMcpd.py
pytest tests/unit/ -x -q

# 2. Déploiement holmesMcpd.py
rsync -av --rsync-path="sudo rsync" \
  resources/holmesMcpd/holmesMcpd.py \
  gtillit@Jeedom:/var/www/html/plugins/holmesMcp/resources/holmesMcpd/holmesMcpd.py

# 3. Redémarrage daemon via Jeedom
ssh gtillit@Jeedom "sudo -u www-data php -r \
  \"require_once '/var/www/html/core/php/jeedom.inc.php'; \
    holmesMcp::deamon_stop(); sleep(2); holmesMcp::deamon_start();\""

# 4. Baseline CPU
ssh gtillit@Jeedom "ps aux | grep holmesMcpd | grep -v grep"

# 5. Récupérer le token Holmes MCP
PID=$(ssh gtillit@Jeedom "sudo cat /tmp/jeedom/holmesMcp/holmesMcpd.pid")
TOKEN=$(ssh gtillit@Jeedom "sudo cat /proc/$PID/cmdline | tr '\0' '\n' | grep -A1 '^--apikey$' | tail -1")

# 6. Déployer les scripts smoke
scp /tmp/smoke_mcp_clean.py gtillit@Jeedom:/tmp/
scp /tmp/smoke_close_wait_trigger.py gtillit@Jeedom:/tmp/

# 7. Smoke propre → 27 tools, CPU stable
ssh gtillit@Jeedom "sudo -u www-data python3 /tmp/smoke_mcp_clean.py http://127.0.0.1:8765/mcp $TOKEN"

# 8. Smoke CLOSE-WAIT reproducer
ssh gtillit@Jeedom "python3 /tmp/smoke_close_wait_trigger.py 8765 $TOKEN"
# Attendre 15-30 s
ssh gtillit@Jeedom "ps aux | grep holmesMcpd | grep -v grep"
ssh gtillit@Jeedom "sudo tail -50 /var/log/jeedom/holmesMcp.log | grep close_wait"

# 9. Nettoyage sudo temporaire
dev/remove-sudo-temp.sh
```

---

## 5. DoD J8-4bis

- [ ] `holmesMcpd.py` modifié avec watchdog Level 2 (log `close_wait_cleanup` + `close_wait_sockets_closed`)
- [ ] `ruff check` vert
- [ ] `pytest tests/unit/` vert
- [ ] Smoke propre : 27 tools confirmés, pas de CLOSE-WAIT
- [ ] Smoke CLOSE-WAIT reproducer : log `close_wait_cleanup` présent, CPU daemon < 20%
- [ ] Commit sur `develop`
- [ ] Session doc `2026-05-06-j8-4bis.md` créée
- [ ] `PROJECT_STATE.md` mis à jour

---

## Rappels

- **INTERDIT** : `kill` / `pkill` / `sudo kill` sur la box sans autorisation PO explicite
- **INTERDIT** : `timeout` OS autour des smoke tests (re-déclenche le bug)
- Toujours `dev/remove-sudo-temp.sh` en fin de session
