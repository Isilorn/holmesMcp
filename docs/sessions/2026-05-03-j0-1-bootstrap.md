# Session J0-1 — Bootstrap repo (2026-05-03)

**Jalon** : J0  
**Type** : Bootstrap offline (pas de SSH)  
**Durée** : ~1 session Claude Code

---

## Objectif de la session

Créer la structure complète du repo à partir de zéro : plugin PHP shell, daemon Python POC-ready, gardes-fous credentials, CI GitHub Actions, MkDocs. Trancher les décisions 🔵 réalisables offline (D9.1, D11.6, D10.3, D12.6).

---

## Artefacts produits

### Structure plugin

```
plugin_info/info.json, packages.json, install.php, changelog.md
core/class/holmesMcp.class.php          ← PHP daemon hooks complets
core/ajax/holmesMcp.ajax.php
core/php/holmesMcp.inc.php, jeeholmesMcp.php
desktop/php/holmesMcp.php               ← UI config (port, tokens par user)
desktop/js/holmesMcp.js
resources/holmesMcpd/holmesMcpd.py      ← Daemon entrypoint POC-ready
resources/holmesMcpd/mcp_server.py      ← FastMCP + tool hello (POC J0)
resources/holmesMcpd/_core/             ← auth, db, api, logs (stubs J1)
resources/holmesMcpd/_domain/           ← sanitize (stub J2)
resources/holmesMcpd/jeedom/jeedom.py   ← Helper Jeedom (jeedom_com, socket)
resources/requirements.txt
```

### Config / CI

```
.gitignore, .gitattributes, pyproject.toml (ruff + pytest)
.github/workflows/ci.yml (lint ruff + unit + integration Docker MySQL)
.github/workflows/docs.yml (MkDocs gh-pages)
mkdocs.yml (Material, fr, dark mode, nav 12 sections)
docs/user/index.md
README.md (market-friendly)
```

### Gardes-fous

```
.git/hooks/pre-commit   ← scan IP privées, tokens, passwords (regex 6 patterns)
.git/hooks/pre-push     ← second scan sur commits à pousser
```

---

## Décisions tranchées

| Décision | Choix | ADR |
| --- | --- | --- |
| D9.1 — HTTP localhost | `httpx` | ADR-0002 proposed |
| D9.1 — Parsing SQL | `sqlparse` | ADR-0002 proposed |
| D9.1 — Logging structuré | `structlog` | ADR-0002 proposed |
| D11.6 — Lint/format | `ruff` | ADR-0002 proposed |
| D10.3 — docs/ localisation | Embarquée dans `main` | ADR-0014 proposed |
| D12.6 — Stack doc | MkDocs Material + docs.yml CI | ADR-0014 proposed |

---

## Décisions reportées à J0-2 (SSH requis)

| Décision | Pourquoi SSH |
| --- | --- |
| D3.2 — Port défaut | `netstat` sur box pour éviter collision plugins |
| D4bis.1 — Driver MySQL | Test PyMySQL + `CREATE USER` privilege sur box réelle |
| D2.4 — Isolation venv | Vérification `system::getCmdPython3()` sur Jeedom 4.5 |
| D1.2 — Version spec MCP | Vérification SDK `mcp` version installée |

---

## POC J0-2 — Checklist pré-SSH

- [ ] Snapshot Proxmox du conteneur Jeedom (PO — ~30s)
- [ ] Connexion SSH `Jeedom` opérationnelle (Claude Code)

## Prochaine session J0-2

1. SSH → `netstat -tlnp` → choisir port défaut (D3.2)
2. SSH → `cat common.config.php` + `mysql -e 'CREATE USER...'` → D4bis.1 + D4bis.2
3. SSH → install plugin (SCP zip) → démarrage daemon → vert UI Jeedom
4. SSH → test MCP Inspector (tools/list retourne `hello`)
5. Bilan → ADR-0018 draft

---

## Actions SSH réalisées cette session

Aucune — session 100% offline.

---

## Notes techniques

- `holmesMcp.class.php` : utilise `system::getCmdPython3(__CLASS__)` pour venv Jeedom 4.5+ — à valider en J0-2
- `holmesMcpd.py` : transport `streamable-http` via `FastMCP.run()` — API exacte à valider contre SDK installé en J0-2
- `mcp_server.py` : un seul tool `hello` pour le POC — 25 tools V1 à partir de J3
- `jeedom.py` : implémentation minimale (jeedom_com + jeedom_socket stub) — socket optionnelle V1
